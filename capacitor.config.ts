import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'codes.heidi.appetized',
  appName: 'Appetized',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
